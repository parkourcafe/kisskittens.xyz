# kisskittens.xyz

Static site for `kisskittens.xyz`.

The repository root is the deployable web root. Upload or sync these files directly into the hosting document root, for example `public_html`.

Entry point: `index.html`.
